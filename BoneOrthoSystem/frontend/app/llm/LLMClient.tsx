// "use client";

// import React from "react";

// // ✅ 這裡直接把你原本 app/llm/page.tsx 的內容「整個搬過來」
// // 保留你原本的 useSearchParams()、狀態、UI 都在這裡跑

// export default function LlmClient() {
//   // ⚠️ 把原本 page.tsx 內的所有程式貼在這裡
//   // return (...原本的 JSX...);

//   return (
//     <div className="p-6 text-white">
//       你還沒把原本 /llm page 的內容搬進來（把原本 page.tsx 內容貼到這裡）
//     </div>
//   );
// }
