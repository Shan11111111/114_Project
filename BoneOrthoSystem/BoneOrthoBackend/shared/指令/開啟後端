# 先切換到專案目錄
cd C:\Users\IM43LLM\Desktop\114_Project\BoneOrthoSystem\BoneOrthoBackend
# 建虛擬環境(還沒建再建)
py -3.11 -m venv .venv
# 啟動虛擬環境
.venv\Scripts\activate

# 安裝必要套件(如果還沒安裝過的話)
py -m pip install -U pip wheel
py -m pip install ultralytics
py -m pip install -U pip
py -m pip install "uvicorn[standard]" fastapi
pip install python-multipart
py -m pip install pyodbc
py -m pip install qdrant-client
py -m pip install langchain-community pypdf
python -m pip install openai python-dotenv
pip install pymupdf
pip install PyPDF2
pip install pdfplumber
pip install docx
pip install exceptions

pip uninstall -y docx
pip install -U python-docx

pip install openpyxl
pip install reportlab

pip install python-docx python-pptx pymupdf




# 啟動後端服務
py -m uvicorn app:app --reload --port 8000


# 停止後端服務時，按 Ctrl + C 即可
# 停止後，若要離開虛擬環境，輸入 deactivate 即可

http://127.0.0.1:8000/s2x/docs

curl -X POST http://127.0.0.1:8000/materials/upload ^
  -F "file=@C:\Users\IM43LLM\Desktop\test.pdf" ^
  -F "title=測試教材_脊椎壓迫性骨折" ^
  -F "type=pdf" ^
  -F "language=zh-TW" ^
  -F "style=edu" ^
  -F "user_id=teacher01" ^
  -F "bone_id=" ^
  -F "bone_small_id="
