# 先切換到專案目錄
cd C:\Users\IM43LLM\Desktop\114_Project\BoneOrthoSystem\BoneOrthoBackend
# 建虛擬環境(還沒建再建)
py -3.11 -m venv .venv
# 啟動虛擬環境
.venv\Scripts\activate

# 安裝必要套件(如果還沒安裝過的話)
py -m pip install -U pip wheel
py -m pip install ultralytics
py -m pip install -U pip
py -m pip install "uvicorn[standard]" fastapi
pip install python-multipart
py -m pip install pyodbc
py -m pip install qdrant-client
py -m pip install langchain-community pypdf
python -m pip install openai python-dotenv
pip install pymupdf
pip install PyPDF2
pip install pdfplumber
pip install docx
pip install exceptions
pip install openai python-dotenv qdrant-client

pip uninstall -y docx
pip install -U python-docx

pip install openpyxl
pip install reportlab

pip install python-docx python-pptx pymupdf

pip install -U qdrant-client

pip install passlib[bcrypt] python-jose[cryptography]

pip install "pydantic[email]"
pip show python-jose
pip install -U "python-jose[cryptography]"


# 啟動後端服務(現在不用這個!!!!!不要用這個開!!!!!要用連外網的指令開)
py -m uvicorn app:app --reload --port 8000

用這個!!
py -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload

# 停止後端服務時，按 Ctrl + C 即可
# 停止後，若要離開虛擬環境，輸入 deactivate 即可

http://127.0.0.1:8000/s2x/docs

curl -X POST http://127.0.0.1:8000/materials/upload ^
  -F "file=@C:\Users\IM43LLM\Desktop\test.pdf" ^
  -F "title=測試教材_脊椎壓迫性骨折" ^
  -F "type=pdf" ^
  -F "language=zh-TW" ^
  -F "style=edu" ^
  -F "user_id=teacher01" ^
  -F "bone_id=" ^
  -F "bone_small_id="

____________________________________________________________________
--------------------------------------------------------------------

cd C:\Users\IM43LLM\Desktop\114_Project\BoneOrthoSystem\BoneOrthoBackend
py -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload

(v)現在用這個2026/03/11
連外網(meta Quest 3要連接):要同一個網路
py -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload

//測試可否連外
Test-NetConnection 140.136.155.157 -Port 8000
--------------------------------------------------------------------
____________________________________________________________________

IIS部屬
py -m uvicorn app:app --host 127.0.0.1 --port 8000

新server:

還沒建在那上面




skiii-proj-hJ22meT4oI3kE9mmI2MX29Z56mz5meiEcB2iah04Da6y_8HRYqTb19Fgn5gjqta-DAo7pi5qjdT3BlbkFJcNeLCqMRNdw2lAd3wgkzoU59fpwIMqeFs6__KfiPyhF1H_J3K0O9Cvzxgc5O4B9lalZNWiUEYA123









